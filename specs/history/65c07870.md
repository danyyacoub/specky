# Commit 65c07870

- **Date:** 2026-09-12T22:46:04+02:00
- **Author:** dany <dany.yacoub@gmail.com>
- **Message:** Serve the viewer from the chat server, with a configurable origin policy

Viewer + chat now serve from same port. `[serve]` config added (specky.toml) — `host`, `allow_origins` (default `["*"]`), optional `token`. Disallowed origins get 403 no CORS, allowed ones echo back + `Vary: Origin`. Static files skip token gate (pages can't send headers on their own `<link>`/`<script>` loads). Backwards compat: defaults unchanged. Added `--host` flag, warning when binding to non-loopback. Docs + spec updated.
