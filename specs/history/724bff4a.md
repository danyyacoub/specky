# Commit 724bff4a

- **Date:** 2026-09-11T14:37:38+02:00
- **Author:** dany <dany.yacoub@gmail.com>
- **Message:** Add specky sync; fix git hook needing specky on global PATH

Git hook required `specky` on global PATH; previous install (project `.venv` only) made hook silently no-op. Fixed via `uv tool install --editable`. Added `specky sync` command: generates micro-docs for any commit missing one (existing history or hook misses). Idempotent—safe to re-run. Updated README with proper install steps.
