---
sha: 899cc414fa0232bb6c4f828e01b57fa50149b9e4
---

# Commit 899cc414

- **Date:** 2026-09-12T23:18:35+02:00
- **Author:** dany <dany.yacoub@gmail.com>
- **Message:** Add `specky doctor` so an installed user can diagnose their setup

Moved doctor diagnostics from shell script to Python (`src/specky/doctor.py`) so installed users can run `specky doctor` directly instead of needing checkout access. `scripts/doctor.sh` now wraps the Python version, keeping the documented entry point working. Checks are free/fast (no provider calls, bounded history scan), exit 1 only on true failures, and 0 on unconfigured repos so it drops into CI as-is.
