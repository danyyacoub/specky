---
sha: 02a963634bd458e1249013ec5749bf0d7ab1a167
---

# Commit 02a96363

- **Date:** 2026-09-21T13:56:45+02:00
- **Author:** dany <dany.yacoub@gmail.com>
- **Message:** feat(rendering): blend diagrams into the page background

Changed the Mermaid diagram figure to blend into the page instead of sitting in a distinct framed box: swapped `--diagram-bg` from `--surface-secondary` to `--surface` and removed the border on `figure.flow`, so the diagram's backdrop matches a node's color. To keep the diagram from pulling focus, dialed the colored radial-gradient washes way down (e.g. 16% → 4%, 14% → 3%, 12% → 3%), turning them into a subtle tint. As a follow-up fix in the reduced-transparency fallback, pointed `--diagram-glass-group` at `--surface-secondary` so solid subgraph nodes still contrast against the now-same-colored backdrop.
