---
sha: 77225b3689f4f10b79cc6a2cb388439cff205140
---

# Commit 77225b36

- **Date:** 2026-09-17T10:53:25+02:00
- **Author:** dany <dany.yacoub@gmail.com>
- **Message:** docs: correct the classification doc's frontmatter and graph claims

This commit fixes two documentation errors in the feature-classification doc. First, a stray duplicate frontmatter block (which had been rendering as a horizontal rule and literal `type: feature` text, since the parser only reads the first block) is removed while keeping the effective values, so index output is unchanged. Second, the doc's description of `specky graph` is corrected: a shared tag only draws an edge from a workflow to a feature (so two features tagged `billing` are not linked), and the graph carries no commits; it also clarifies that `commit_links` pairs a commit with a doc rather than its tags. A pointer to `catalog/feature-graph.md` is added so that file owns the behavior and the two docs don't drift.
