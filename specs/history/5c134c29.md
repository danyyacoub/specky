# Commit 5c134c29

- **Date:** 2026-09-11T15:43:45+02:00
- **Author:** dany <dany.yacoub@gmail.com>
- **Message:** Fix FTS5 syntax errors on plain-text search queries; dedupe micro_docs schema and diff-truncation constant

Fixed FTS5 crashes on queries with special chars (., -, *) by centralizing safe query builder as fts_match_query() in db.py. Both indexer.search() and chat_server.retrieve_context() now use it, changing multi-word queries to OR (broader recall instead of AND). Also deduped micro_docs schema—record_micro_doc() now uses shared db.connect(). Centralized 8000-char diff truncation limit as DIFF_TRUNCATE_CHARS constant to remove copy-paste.
