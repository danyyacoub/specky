---
sha: 74d2143231854e98569787c1c88fe22b126555ae
---

# Commit 74d21432

- **Date:** 2026-09-15T13:56:08+02:00
- **Author:** dany <dany.yacoub@gmail.com>
- **Message:** Survive a real repo's docs tree and toolchain

Four production fixes from trialling specky on 13.8k-commit repo. Un-stage docs when pre-commit rejects them (so they don't bleed into next commit under wrong author). Switch JSON parsing to `raw_decode` to catch models adding trailing data (silent failures became whole-doc rewrites). Fix MODULES.md heading matching to avoid duplicate rows from hand-written headings. Add `doctor` check for specky's own runtime imports — editable installs resolve deps once, so new dependencies in `pyproject.toml` aren't available until `--force` reinstall (docs would die on missing `markdown` while doctor passed).
