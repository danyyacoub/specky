# Commit ee5c73c4

- **Date:** 2026-09-12T21:15:21+02:00
- **Author:** dany <dany.yacoub@gmail.com>
- **Message:** Add #module/#feature scoping and HTML snippets to the Ask widget

**Add scoping and HTML rendering to Ask widget**

Chat now supports `#module:<domain>` and `#feature:<slug>` tokens to scope FTS5 retrieval to specific doc sections, with fallback to unscoped search when no match. Responses can optionally include one fenced ```html block for diagrams/tables, rendered client-side in a sandboxed iframe (no scripts, CSP meta injected). UI adds mention dropdown with doc/feature autocomplete on '#'. System prompt updated to encourage HTML snippets when they clarify answers.
