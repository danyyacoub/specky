# Commit 5171e78b

- **Date:** 2026-09-12T22:48:29+02:00
- **Author:** dany <dany.yacoub@gmail.com>
- **Message:** Don't write a second frontmatter block when the model echoes one

Model output went straight to `frontmatter.render()`, which prepends a block unconditionally. When model echoed back frontmatter from commit diffs, docs ended up with two blocks — first one (the one readers saw) was the model's fabrication, not the real metadata.

Fix: parse and discard model's frontmatter before rendering. Real block comes from this run's classification, not model echo.
