__pycache__/
*.pyc
.venv/
.venv*/
dist/
build/
*.egg-info/
.specky/
specky.toml
# Provider API keys. specky reads them from the environment by name (`[ai] api_key_env`)
# and never stores a key itself, so nothing here ever needs to be committed.
.env
.env.*
!.env.example
.DS_Store
.idea/
.serena/
node_modules/
# `specky tests` output. Users commit theirs; here it's just this repo dogfooding itself, and 126
# skipped scaffolds duplicating tests/ would drown out scripts/test.sh.
tests/spec/
