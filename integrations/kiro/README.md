# Kiro integration

Not yet written — tracked as "later / not in v1 scope" in the project plan. Will contain the Kiro workspace/user MCP JSON snippet pointing at the same `specky-mcp` command, plus a note that the shared skill can be copied to `.kiro/skills/document-domain/` (Kiro follows the same open Agent Skills standard as Claude Code's `SKILL.md`).
